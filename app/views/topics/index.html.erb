<h1>Topics#index</h1>

<div>
  <%= link_to '新規スレッド作成', new_topic_path %>
</div>

<div>
  <%= link_to '投稿', posts_path %>
</div>

<% @topics.each do |topic| %>
  <div class="topic-row">
    <P class=title><%= topic.title %></p>
  </div>
<% end %>